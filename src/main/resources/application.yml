okta:
  oauth2:
    issuer: ${OKTA_ISSUER}
    client-id: ${OKTA_CLIENT_ID}
    client-secret: ${OKTA_CLIENT_SECRET}

cloudinary:
  cloud-name: ${C_CLOUD_NAME}
  api-key: ${C_API_KEY}
  api-secret: ${C_API_SECRET}
    
